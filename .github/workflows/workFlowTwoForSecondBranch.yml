# This workflOW created to execute the basketApiTests
name: basketApiRuningTests
on:
  push:
    branches:
      - OWASPbranch
jobs:
  build-and-test:
    name: "second job"
    runs-on: "ubuntu-latest"
    steps:
      - name: "step one"
        run: echo "hello hello"
